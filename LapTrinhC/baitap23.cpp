//3. Sắp xếp dãy số nguyên gồm n phần tử giảm dần

#include <stdio.h>

/*int main() {
	int a[100], n;
	printf("\nNhap vao so luong phan tu: ");
	scanf_s("%d", &n);
	for (int i = 0; i < n; i++) {
		printf("\nNhap a[%d]: ", i);
		scanf_s("%d", &a[i]);
		if (a[i] < a[i + 1]) {
			printf("%d", a[i]);
		}
	}
}*/