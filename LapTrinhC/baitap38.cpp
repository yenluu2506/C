/*1. Viết chương trình nhập vào từ bàn phím độ dài hai cạnh của hình chữ nhật.Kiểm tra nếu độ
dài nhập vào dương thì tính và in ra màn hình chu vi, diện tích của hình chữ nhật đó, ngược
lại, nếu độ dài nhập vào âm thì thông báo không tính được.*/


/*#include <stdio.h>

int main() {
	int cv, dt, cd, cr;
	printf("Nhap vao chieu dai va chieu rong: ");
	scanf_s("%d %d", &cd, &cr);
	if (cd > 0 && cr > 0) {
		cv = (cd + cr) * 2;
		printf("\nChu vi cua hinh chu nhat la: %d", cv);
		dt = cd * cr;
		printf("\nDien tich cua hinh chu nhat la: %d", dt);
	}
	else
		printf("Khong tinh duoc dap an.");
}*/