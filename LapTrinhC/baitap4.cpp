//4. Nhập vào 1 số gồm 3 chữ số, yêu cầu xuất ra tổng chữ số của nó ra màn hình (Ví dụ: Nhập vào 123 -> Xuất ra màn hình: 1+2+3 = 6)

/*#include <stdio.h>
int main() {
	int a;
	printf(" Nhap vao so co 3 chu so: ");
	scanf_s("%d", &a);
	int s = 0;
	while (a / 10 > 0) {
		s = s + a % 10;
		a = a / 10;
	}
	s = s + a;
	printf(" Tong cac chu so la %d ", s);
} */