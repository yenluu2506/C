// 9. Viết chương trình nhập một mảng một chiều gồm n số nguyên. Tính và in ra màn hình tổng của các phần tử trong mảng.

/*#include<stdio.h>
void Nhap(int a[], int& n) {
	printf("Nhap so luong cho mang: ");
	scanf_s("%d", &n);
	for (int i = 0; i < n; i++) {
		printf("Nhap gia tri cho a[%d]: ", i);
		scanf_s("%d", &a[i]);
	}
}

void Tong(int a[], int n) {
	int S = 0;
	for (int i = 0; i < n; i++) {
		S += a[i];
	}
	printf("S = %d", S);
}

int main() {
	int a[100], n;
	Nhap(a, n);
	Tong(a, n);
}*/