//Viết chương trình nhập một mảng một chiều n số thực.Đếm và thông báo ra màn hình trong mảng có bao nhiêu phần tử âm.

/*#include <stdio.h>

void Nhapmang(int a[], int n) {
	for (int i = 0; i < n; i++) {
		printf("a[%d]: ", i);
		scanf_s("%d", &a[i]);
	}
}
void Xuatmang(int a[], int n) {
	for (int i = 0; i < n; i++) {
		printf("%5d", a[i]);
	}
}
void Sophantuam(int a[], int n) {
	int dem = 0;
	for (int i = 0; i < n; i++) {
		if (a[i] < 0)
			dem++;

	}
}

int main() {
	int a[1000];
	int n;
	printf("Nhap vao so luong phan tu: ");
	scanf_s("%d", &n);
	Nhapmang(a, n);
	printf("\nMang da nhap la: ");
	Xuatmang(a, n);
	printf("\nSo luong phan tu am la: ");
	Sophantuam(a, n);
}*/