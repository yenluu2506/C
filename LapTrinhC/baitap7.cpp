//7. Nhập vào một số là số giây, đổi số giây này ra giờ phút giây và xuất theo dạng gio : phut:giay, mỗi thành phần có 2 chữ số(Ví dụ 3661 = 01:01 : 01).

/*#include <stdio.h>
int main() {

	int a, giay, phut, gio;
	printf("Nhap vao so giay: ");
	scanf_s("%d", &a);
	giay = a / 3600;
	phut = (a - giay * 3600) / 60;
	gio = a - giay * 3600 - phut * 60;
	printf("Doi giay ra la %d:%d:%d ", giay, phut, gio);
}
*/