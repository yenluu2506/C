#include <stdio.h>
#include <math.h>

/*int PTBH(float a, float b, float c, float& x1, float& x2) {
	float delta = b * b - 4 * a * c;
	if (delta < 0)
		return 0;
	else if (delta == 0) {
		x1 = -b / (2 * a);
		return 1;
	}
	else {
		x1 = (-b + sqrt(delta)) / (2 * a);
		x2 = (-b - sqrt(delta)) / (2 * a);
		return 2;
	}
}

int main() {
	float a, b, c, x1, x2;
	do {
		printf("Nhap a, b, c: ");
		scanf_s("%f %f %f", &a, &b, &c);

	} while (a == 0);
	if (PTBH(a, c, x1, x2) == 0)
		printf("Phuong trinh vo nghiem");
	else if (PTBH(a, b, c, x1, x2)==1)

}*/