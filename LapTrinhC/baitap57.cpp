// 10. Viết chương trình nhập một mảng một chiều gồm n số nguyên dương. Tính và thông báo ra màn hình tổng các phần tử có giá trị chẵn trong mảng.

/*#include<stdio.h>
void nhapmang(int a[], int n)
{
    int i;
    for (i = 0; i < n; i++) {
        printf("\nNhap phan tu thu %d: ", i);
        scanf_s("%d", &a[i]);
    }
}

int tongchan(int a[], int n)
{
    int i, s = 0;
    for (i = 0; i < n; i++) if (a[i] % 2 == 0) s += a[i];
    return s;
}
int main()
{
    int n;
    int a[500];
    printf("\nnhap so phan tu trong mang: ");
    scanf_s("%d", &n);
    nhapmang(a, n);
    printf("\nTong cac phan tu chan trong mang: %d", tongchan(a, n));
}*/

