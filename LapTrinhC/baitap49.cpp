//1. Viết chương trình nhập vào 1 dãy số từ a1, a2, …, an. In dãy số vừa nhập ra màn hình theo nguyên tắc 1 dòng gồm 10 phần tử, dòng cuối có thể có ít hơn 10 phần tử.

/*#include <stdio.h>

void Nhapmang(int a[], int n) {
	for (int i = 0; i < n; i++) {
		printf("a[%d]:", i);
		scanf_s("%d", &a[i]);
	}
}
void Xuatmang(int a[], int n) {
	for (int i = 0; i < n; i++) {
		printf("%d", a[i]);
	}
}
int main() {
	int a[1000];
	int n;
	//printf("Nhap n:");
	//scanf_s("%d", &n);
	for (int i = 0; i < (n / 10); i++) {
		printf("Nhap n:");
		scanf_s("%d", &n);
	}
	Nhapmang(a, n);
	Xuatmang(a, n);
	printf("Day so vua nhap ra man hinh la: ", n);
}*/

